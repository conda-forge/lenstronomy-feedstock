{% set name = "lenstronomy" %}
{% set version = "1.11.4" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/lenstronomy-{{ version }}.tar.gz
  sha256: d72e34e1182e91414acdc22ac252f4f3e332b62a6d6e1acce63e0ab1c280a501


build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - pytest
    - python >=3.7
  run:
    - astropy
    - configparser
    - corner >=2.2.1
    - h5py
    - matplotlib-base
    - mpmath
    - multiprocess >=0.70.8
    - numba >=0.43.1
    - numpy >=1.13
    - python >=3.7
    - pyxdg
    - pyyaml
    - schwimmbad
    - scikit-image
    - scikit-learn
    - scipy >=0.19.1

test:
  imports:
    - lenstronomy
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/lenstronomy/lenstronomy
  summary: Strong lens modeling package.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - sibirrer
