{% set name = "lenstronomy" %}
{% set version = "1.12.4" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/lenstronomy-{{ version }}.tar.gz
  sha256: 07f23fb0a7518c749037685b3c9f6f7a1a077947a2667f1599306f408a79336b


build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - pytest
    - python {{ python_min }}
  run:
    - astropy-base
    - configparser
    - corner >=2.2.1
    - h5py
    - matplotlib-base
    - mpmath
    - multiprocess >=0.70.8
    - numba >=0.43.1
    - numpy >=1.13
    - python >={{ python_min }}
    - pyxdg
    - pyyaml
    - schwimmbad
    - scikit-image
    - scikit-learn
    - scipy >=0.19.1

test:
  imports:
    - lenstronomy
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/lenstronomy/lenstronomy
  summary: Strong lens modeling package.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - sibirrer
